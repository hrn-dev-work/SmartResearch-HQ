# Dependencies
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
.env.*.local

# Build / cache
.next/
out/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
.DS_Store

# Logs & runtime
*.log
playwright-report/
test-results/
.coverage
htmlcov/

# Agent / Cursor debug output (local)
merge-result.txt
.git-status-tmp.txt
pytest-out.txt
stash-pop.txt
git-st.txt
commit-check.txt
branch-state.txt
rebase-state.txt
final-status.txt
diff-detail.txt
diff-stat.txt
*-log.txt
*-state.txt
show-*.txt

# Secrets (never commit)
credentials.json
service-account*.json
*.pem

# Playwright browsers (local install)
/playwright/.cache/

# Cursor agent config — local only (rules, skills, hooks; never push to public clone)
.cursor/

# Generated from local .cursor/skills (scripts/sync-skill-prompt-table.py)
docs/prompt-templates.md

# Git / agent scripts — local only (public clone uses git-ship, ci-check, render-*, ensure-pr)
scripts/sync-skill-prompt-table.py
scripts/git-first-pr.sh
scripts/git-push-pr.sh
scripts/update-open-pr-bodies.sh
scripts/sync-github-labels.py

# Agent / Git playbook — local only (public clone には含めない。裏用)
AGENTS.md
docs/agent-setup.md
docs/git-workflow.md

# Local docs index and notes (see docs/local/README.md on developer machine)
docs/local/
frontend/AGENTS.md
frontend/CLAUDE.md

# Agent debug output (local)
.ci-investigation-gh.txt
